/**
 * Carousel Constants
 *
 * Centralized configuration for carousel components.
 */

/** Banner carousel autoplay delay in milliseconds */
export const BANNER_AUTOPLAY_DELAY_MS = 4000;

/** Room carousel autoplay delay in milliseconds */
export const ROOM_AUTOPLAY_DELAY_MS = 3000;

/**
 * Rooms taken off the top of the home page-1 payload for the carousel.
 * The bottom grid starts at this offset — the two must never overlap.
 */
export const HOME_CAROUSEL_ROOM_COUNT = 5;
